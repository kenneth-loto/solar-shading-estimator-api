model Site {
  id             String     @id @default(cuid())
  name           String
  latitude       Float
  longitude      Float
  panelTilt      Float
  panelAzimuth   Float
  systemSize     Float
  horizonProfile Json
  createdAt      DateTime   @default(now())
  updatedAt      DateTime   @updatedAt
  analyses       Analysis[]
}
